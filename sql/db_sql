create database warsztat2 character set utf8mb4 collate utf8mb4_unicode_ci;

create table users (
                       id int auto_increment,
                       username varchar(255) unique not null,
                       email varchar(255) unique not null,
                       password varchar(255) not null,
                       primary key(id)
);

